# =============================================================================
# ADS OPTIMIZER - CONFIGURAÇÕES DO AMBIENTE
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO SERVIDOR
# -----------------------------------------------------------------------------

# Porta do servidor (padrão: 3001)
PORT=3001

# Ambiente (development, production, test)
NODE_ENV=development

# URL do frontend para CORS
FRONTEND_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# BANCO DE DADOS (POSTGRESQL + PRISMA)
# -----------------------------------------------------------------------------

# URL de conexão do PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/nome_banco?schema=public
# Para Neon: postgresql://user:password@ep-xxxxx.us-east-2.aws.neon.tech/ads_optimizer?sslmode=require
DATABASE_URL=postgresql://postgres:sua_senha@localhost:5432/adsoptimizer?schema=public

# -----------------------------------------------------------------------------
# AUTENTICAÇÃO JWT
# -----------------------------------------------------------------------------

# Chave secreta para geração de tokens JWT
# Use uma string longa e aleatória em produção
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_jwt_super_secreta_aqui

# Tempo de expiração do token (ex: 7d, 24h, 1h)
JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# GOOGLE ADS API - CREDENCIAIS OAUTH2
# -----------------------------------------------------------------------------
# 
# COMO CONFIGURAR:
# 
# 1. Acesse Google Cloud Console: https://console.cloud.google.com
# 2. Crie um novo projeto ou selecione um existente
# 3. Ative a "Google Ads API" em APIs & Services > Library
# 4. Vá em APIs & Services > Credentials
# 5. Clique em "Create Credentials" > "OAuth client ID"
# 6. Selecione "Web application" ou "Desktop app"
# 7. Configure as URIs de redirecionamento (para web):
#    - http://localhost:3001/api/google-ads/auth/callback (desenvolvimento)
#    - https://seudominio.com/api/google-ads/auth/callback (produção)
# 8. Copie o Client ID e Client Secret gerados
# 
# DEVELOPER TOKEN:
# 1. Acesse sua conta Google Ads: https://ads.google.com
# 2. Vá em Ferramentas e Configurações > Central de API
# 3. Solicite ou copie seu Developer Token
# Obs: Para testes, use token de teste (limite de operações)
# 
# LOGIN CUSTOMER ID (para contas MCC):
# Se você usa uma conta gerenciadora (MCC), informe o ID da conta MCC aqui
# Formato: 1234567890 (sem hífens)
# -----------------------------------------------------------------------------

# Client ID do OAuth2 (Google Cloud Console)
GOOGLE_ADS_CLIENT_ID=seu_client_id.apps.googleusercontent.com

# Client Secret do OAuth2 (Google Cloud Console)
GOOGLE_ADS_CLIENT_SECRET=seu_client_secret

# Developer Token (Google Ads > Ferramentas > Central de API)
GOOGLE_ADS_DEVELOPER_TOKEN=seu_developer_token

# Refresh Token (obtido após fluxo OAuth2)
# Use o endpoint /api/google-ads/auth/url para iniciar o fluxo
GOOGLE_ADS_REFRESH_TOKEN=seu_refresh_token

# ID da conta gerenciadora MCC (opcional, sem hífens)
# Necessário se você acessa contas através de uma MCC
GOOGLE_ADS_LOGIN_CUSTOMER_ID=1234567890

# URI de redirecionamento para OAuth2 callback
GOOGLE_ADS_REDIRECT_URI=http://localhost:3001/api/google-ads/auth/callback

# -----------------------------------------------------------------------------
# OPENAI API
# -----------------------------------------------------------------------------
# 
# COMO CONFIGURAR:
# 1. Acesse: https://platform.openai.com
# 2. Vá em API Keys e crie uma nova chave
# 3. Copie a chave gerada (começa com sk-)
# -----------------------------------------------------------------------------

# Chave da API OpenAI para recomendações com IA
OPENAI_API_KEY=sk-sua_api_key_aqui
